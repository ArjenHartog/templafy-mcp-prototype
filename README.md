# Templafy MCP Prototype

Clickable prototype for the Templafy MCP connector sign-in and free product flow.

Open `index.html` from GitHub Pages. The root page redirects to the flow map in `prototype/index.html`.
